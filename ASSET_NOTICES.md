# Camera Operator project-page asset notices

The bundled `camera-operator-mm26.pdf` is the Camera Operator camera-ready
article. Its first-page rights block specifies the Creative Commons Attribution
4.0 International license (CC BY 4.0).

The following files are resized and WebP-converted reproductions of figures in
that article:

| Project-page file | Article source |
| --- | --- |
| `assets/teaser.webp` | Figure 1 (`CoverFigure.pdf`) |
| `assets/method.webp` | Figure 4 (`cameraoperator_method_overview.pdf`) |
| `assets/comparison.webp` | Figure 5 (`contrast.pdf`) |
| `assets/downstream.webp` | Figure 6 (`bbox_video_two_rows.png`) |

The RGB frames appearing in Figures 1 and 6 are downstream video-generation
examples reported and attributed in the article. Camera Operator supplies the
camera trajectory; it does not itself generate the RGB appearance. No external
model weights, source datasets, Unreal projects, Fab assets, or third-party
software are distributed by this project-page bundle.

`assets/favicon.svg`, the HTML, CSS, and JavaScript are original project-page
materials. `assets/og-cameraoperator.jpg` is an original project-page social
card incorporating a resized crop of the CC BY 4.0 Figure 1 reproduction.
Code, model weights, datasets, and third-party components are not licensed by
this notice; any future release must carry its own license and attribution
terms.
